
<%= form_for @appointment do |f|%>
  <p>
    <%= f.text_field :date, id: 'datepicker', size:10 %>
    <%= f.time_select :date, :ignore_date => false %>
    <!-- # Added later. not shown in screenshot -->
  </p>
  <%= f.collection_select :doctor_id, @doctors, :id, :name %>
  <%= f.collection_select :patient_id, @patients, :id, :name %>
  <p>
    <%= f.text_area :notes, placeholder: "Notes" %>
  </p>
  <%= f.button "Schedule Appointment", type: :submit %>
<% end %>
